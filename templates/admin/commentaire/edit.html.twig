{% extends 'admin/base.html.twig' %}

{% block title %}Modification du commentaire n° {{commentaire.id}}{% endblock %}


{% block body %}

    <div class="container">
        <h1 class="my-5">Modification du commentaire n° {{commentaire.id}}</h1>

        <div class="alert alert-info">
            <strong>Annonce liée : </strong> {{commentaire.annonce.title}}<br>
            <strong>Auteur : </strong> {{commentaire.auteur.fullName}}<br>
            <strong>Date de création : </strong> {{commentaire.createdAt|date('d/m/Y H:i')}} <br>
            <strong>Note donnée : </strong>{{commentaire.note}}
        </div>

        {{ form_start(form) }}

        {{ form_widget(form) }}

        <button type="submit" class="btn btn-primary">
            <i class="fas fa-check"></i>
            Enregistrer les modifications
        </button>

        {{ form_end(form) }}
    </div>

{% endblock %}